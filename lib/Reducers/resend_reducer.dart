
/*import 'package:carpooling/Model/resend/resend_state.dart';

ResendState resendStateReducer(ResendState State,action){
  return ResendState(details: itemReducer(state.details, action));
}

List<User>itemReducer(List<User> state, action)
{
  if(action is AddAction){
    return []..addAll(state);
    //..add(User.fromJson(dummyjson));

  }
  return state;
}*/